@using DiscordSecretSanta.Core.ViewModels

<DiscordBlock User="@User" ShowIsAdmin="false">
    <StatusBadge Status="@User.SecretSantaStatus"></StatusBadge>
</DiscordBlock>

@code {
    [Parameter]
    public UserViewModel User { get; set; }
}